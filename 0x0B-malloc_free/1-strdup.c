#include <stdlib.h>
#include <stdio.h>
#include "main.h"
/**
 *
 *
 *
 *
 */
char *_strdup(char *str)
{
	int size;
	char *free[];

	str = malloc(size * sizeof(*free));
	if (str == NULL)
	{
		return (NULL);
	}
	free(str);
	return (str);
}
