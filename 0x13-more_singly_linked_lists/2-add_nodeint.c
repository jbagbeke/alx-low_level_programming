#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

/**
 *
 *
 *
 *
 */
listint_t *add_nodeint(listint_t **head, const int n)
{
	listint_t *temp;
	listint_t *new;

	*new = malloc(sizeof(listint_t));
	if (new == NULL)
		return (NULL);
	
	*head->temp;
	new->next;
	new->n = n;
	head->new;

	free(new)

	return (new);

}
